// Shared UI utilities
export { cn } from "./lib/utils";

// Components are added incrementally as each step requires them (Steps 5–25).
// shadcn/ui components are added to consuming apps via `npx shadcn@latest add <component>`
// and shared components that need to be reused across both apps live here.
